<%= form_with(model: @page, html: {multipart: true}) do |form| %>
  <% if false %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
        <% page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <% end %>
  <div class="field">
    <%= form.label :subject_id, "Subject" %>
    <%= form.select(:subject_id, @subjects.map {|s| [s.name, s.id]}) %>
  </div>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.text_field :position %>
  </div>
 
   <div class="field">
    <%= form.label :permalink %>
    <%= form.text_field :permalink %>
  </div>
  
  <div class="field">
     
    <%= form.label :visible %>
    <%= form.check_box :visible %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
